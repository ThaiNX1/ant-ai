PORT=8080
NODE_ENV=development

# Root API Key — internal ecosystem only, never expose to end users
ROOT_API_KEY=change-me-to-a-strong-secret-key

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=api_gateway

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
